#include <stdio.h>


/**
 * main - program that prints the alphabet in lowercase and upper case
 *
 * Return: Always 0.
 */

int main(void)
{
	int d;

	for (d = '0'; d <= '9'; d++)
	putchar (d);
	putchar('\n');
	
	return (0);
}
